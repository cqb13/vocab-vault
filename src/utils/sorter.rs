use crate::{Translation, TranslationType};

// E and bellow are uncommon
const FREQUENCY_ORDER: [&str; 10] = ["A", "B", "C", "D", "E", "F", "I", "M", "N", "X"];


pub fn sort_output(translations: Vec<Translation>) {
    
}

fn sort_latin_output() {

}

// english output is already sorted, but we can sort by latin frequency
fn sort_english_output_by_latin_definition() {

}